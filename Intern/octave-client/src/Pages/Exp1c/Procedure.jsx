import React from 'react'

const Procedure = () => {
  return (
    <div>
      <ol className='list-decimal space-y-5 '>
        <li>Select the algorithm which you want to try out.</li>
        <li>Slect all the parameters on the right side.</li>
        <li>After selecting parameters click the generate code to see the code on the left side.</li>
        <li>If you want to download the code click the download button and (click submit and run) to run the code.</li>
        <li>Results will be displayed below the generate code button.</li>
      </ol>
    </div>
  )
}

export default Procedure
